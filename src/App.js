import './App.css';

const App = () => {
  return (
    <h1>hello world</h1>
    // <Route path = '/' component={HomeScreen} exact />
    //   <Route path = '/product/:id' component={ProductScreen} exact />
  );
}

export default App;
